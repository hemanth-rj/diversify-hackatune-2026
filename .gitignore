# Node / frontend
node_modules/
frontend/node_modules/
frontend/dist/
frontend/.vite/
*.log

# Env / secrets — never commit real keys
.env
.env.*
!.env.sample
frontend/.env
frontend/.env.*
!frontend/.env.production   # public URLs only, safe to commit for reproducible builds
backend/.env

# Python (Orkun's backend in this repo)
__pycache__/
*.pyc
.venv/
venv/

# Caches (backend tag/audio/response caches + downloaded audio)
.cache/
.tag_cache/
.audio_cache/
audio/
.pytest_cache/
.ruff_cache/

# OS
.DS_Store
Thumbs.db
