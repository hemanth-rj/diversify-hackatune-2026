# Copy to .env (git-ignored). Used by docker-compose and local runs.
DATABASE_URL=postgres://harvest:harvest@localhost:5432/harvest
# Per-worker knobs (override in docker-compose `command` or on the CLI):
#   --concurrency  simultaneous tracks per worker
#   --rps          download requests/sec per worker (keep rps*replicas polite)
